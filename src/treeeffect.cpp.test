#include <iostream>
#include <map>
#include <string>
#include <vector>

// tree sequencer Treequencer

// A node in the tree
struct Node {
  std::string data;
  std::unordered_map<Node*, int> children;
};

Node* root = new Node{"Root"};

root->children[new Node{"Child 1"}] = 1;
root->children[new Node{"Child 2"}] = 2;
root->children[new Node{"Child 3"}] = 3;

for (const auto& [child, value] : node->children) {
    stack.push_back(child);
}